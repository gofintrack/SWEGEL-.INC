<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Landing Page</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="landing-body">
    <canvas id="bgNetwork"></canvas>
    <div class="blood-layer" id="bloodLayer"></div>

    <header class="top-nav">
      <nav class="menu-wrap">
        <div class="dropdown">
          <button class="dropbtn">Tools</button>
          <div class="dropdown-content">
            <% tools.forEach((item) => { %>
            <a href="<%= item.url %>" target="_blank" rel="noopener noreferrer"><%= item.name %></a>
            <% }) %>
          </div>
        </div>
        <div class="dropdown">
          <button class="dropbtn">Script</button>
          <div class="dropdown-content">
            <% scripts.forEach((item) => { %>
            <a href="<%= item.url %>" target="_blank" rel="noopener noreferrer"><%= item.name %></a>
            <% }) %>
          </div>
        </div>
      </nav>
    </header>

    <main class="hero">
      <h1>SweGeL .Inc</h1>
      <p>
        Premium toolkit for Validator, Email Sender, and Shortlink Generator, plus trusted script
        packages: Xfinity, Chase, and TopperPay in one controlled marketplace.
      </p>
      <a class="buy-now-btn" href="https://t.me/SweGeL_Corps" target="_blank" rel="noopener noreferrer">
        Buy Now
      </a>
    </main>

    <script src="/js/landing.js"></script>
  </body>
</html>
